export interface SocialLink {
    name: string;
    url: string;
    bgColor: string;
    hoverColor: string;
}

export interface Profile {
    name: string;
    username: string;
    image: string;
}
